<UserControl x:Class="Luster.PreviewHost.Fixtures.ThemedSampleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Luster.PreviewHost.Fixtures"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400"
             d:DesignInstance="local:ThemedSampleDesignVm"
             Background="{DynamicResource PrimaryBrush}">
    <StackPanel Margin="16">
        <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="Bold" Foreground="White"/>
        <TextBlock Text="{Binding Subtitle}" FontSize="14" Margin="0,8,0,0" Foreground="White"/>
        <Button Content="{Binding ActionLabel}" Margin="0,12,0,0" Style="{DynamicResource ButtonPrimary}"/>
    </StackPanel>
</UserControl>
