<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFramework>net472</TargetFramework>
		<IsPackable>false</IsPackable>
		<!-- 仅测试 Safety 模块的纯数据/纯逻辑件，不拉起 WPF/设备引擎 -->
	</PropertyGroup>

	<ItemGroup>
		<PackageReference Include="Microsoft.NET.Test.Sdk" />
		<PackageReference Include="xunit" />
		<PackageReference Include="xunit.runner.visualstudio" />
	</ItemGroup>

	<ItemGroup>
		<ProjectReference Include="..\..\Modules\Luster.Module.Motion.Safety\Luster.Module.Motion.Safety.csproj" />
		<!--
			仅测试引用 Handover 件（TES-48），用于跨模块防漂移契约单测
			（HandshakeBit ↔ HandoverSignalBit ↔ 源端枚举三者钉死）。
			非生产跨模块引用：Safety 生产件不依赖 Handover，仅测试项目为校验语义一致而引用。
		-->
		<ProjectReference Include="..\..\Modules\Luster.Module.Motion.Handover\Luster.Module.Motion.Handover.csproj" />
	</ItemGroup>

</Project>
