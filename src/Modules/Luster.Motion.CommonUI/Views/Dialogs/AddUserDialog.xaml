<UserControl
    x:Class="Luster.Motion.CommonUI.Views.Dialogs.AddUserDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:lang="clr-namespace:Luster.Motion.Assests.Langs;assembly=Luster.Motion.Assests"
    xmlns:local="clr-namespace:Luster.Motion.CommonUI.Views.Dialogs"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="{DynamicResource RegionBrush}"
    mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <hc:UniformSpacingPanel Orientation="Vertical" Spacing="10">
            <hc:ComboBox
                MinWidth="220"
                hc:InfoElement.Placeholder="用户角色"
                hc:TitleElement.Title="用户角色"
                hc:TitleElement.TitlePlacement="Left"
                hc:TitleElement.TitleWidth="120"
                DisplayMemberPath="Desc"
                IsEditable="True"
                ItemsSource="{Binding UserRoles}"
                SelectedItem="{Binding CurrentRole}"
                SelectedValuePath="Value" />
            <TextBox
                hc:InfoElement.Necessary="True"
                hc:TitleElement.Title="{lang:Lang Key={x:Static lang:LangKeys.UserName}}"
                hc:TitleElement.TitlePlacement="Left"
                hc:TitleElement.TitleWidth="120"
                Style="{StaticResource TextBoxValidationStyle}"
                Text="{Binding UserName}" />
            <!--<TextBox
                hc:InfoElement.Necessary="True"
                hc:TitleElement.Title="Badge ID"
                hc:TitleElement.TitlePlacement="Left"
                hc:TitleElement.TitleWidth="120"
                Style="{StaticResource TextBoxValidationStyle}"
                Text="{Binding BadgeID}" />-->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <PasswordBox
                    x:Name="oldPassword"
                    Margin="0,0,0,10"
                    hc:InfoElement.Necessary="True"
                    hc:InfoElement.Title="{lang:Lang Key={x:Static lang:LangKeys.OriginalPassword}}"
                    hc:InfoElement.TitlePlacement="Left"
                    hc:TitleElement.TitleWidth="120"
                    PasswordChanged="oldPassword_PasswordChanged"
                    PasswordChar="*"
                    Style="{StaticResource PasswordBoxExtend}"
                    Visibility="{Binding IsEdit, Converter={StaticResource Boolean2VisibilityConverter}}" />
                <PasswordBox
                    x:Name="password"
                    Grid.Row="1"
                    hc:InfoElement.Necessary="True"
                    hc:InfoElement.Title="{lang:Lang Key={x:Static lang:LangKeys.PassWord}}"
                    hc:InfoElement.TitlePlacement="Left"
                    hc:TitleElement.TitleWidth="120"
                    PasswordChanged="password_PasswordChanged"
                    PasswordChar="*"
                    Style="{StaticResource PasswordBoxExtend}" />
            </Grid>
            <PasswordBox
                x:Name="rePassword"
                hc:InfoElement.Necessary="True"
                hc:InfoElement.Title="{lang:Lang Key={x:Static lang:LangKeys.RepeatPassword}}"
                hc:InfoElement.TitlePlacement="Left"
                hc:TitleElement.TitleWidth="120"
                PasswordChanged="rePassword_PasswordChanged"
                PasswordChar="*"
                Style="{StaticResource PasswordBoxExtend}" />
        </hc:UniformSpacingPanel>
        <DockPanel Grid.Row="1" Margin="0,20,0,0">
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Button
                    Width="80"
                    HorizontalAlignment="Right"
                    Command="{Binding CloseCommand}"
                    CommandParameter="cancel"
                    Content="{lang:Lang Key={x:Static lang:LangKeys.Cancel}}" />
                <Button
                    Width="80"
                    Margin="10,0,0,0"
                    HorizontalAlignment="Right"
                    Command="{Binding CloseCommand}"
                    CommandParameter="ok"
                    Content="{lang:Lang Key={x:Static lang:LangKeys.Confirm}}" />
            </StackPanel>
        </DockPanel>
    </Grid>
</UserControl>
