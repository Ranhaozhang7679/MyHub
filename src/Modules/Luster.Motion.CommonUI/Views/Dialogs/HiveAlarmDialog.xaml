<UserControl x:Class="Luster.Motion.CommonUI.Views.Dialogs.HiveAlarmDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Luster.Motion.CommonUI.Views.Dialogs"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Loaded="UserControl_Loaded" >
    <Grid Background="#E15358">
        <hc:Interaction.Triggers>
            <hc:EventTrigger EventName="KeyDown">
                <hc:EventToCommand Command="{Binding ScanCommand}" PassEventArgsToCommand="True" />
            </hc:EventTrigger>
        </hc:Interaction.Triggers>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Background="Gray" />
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>

            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="267*"/>
                <ColumnDefinition Width="229*"/>
                <ColumnDefinition Width="38*"/>
                <ColumnDefinition Width="267*"/>

            </Grid.ColumnDefinitions>
            <Label HorizontalAlignment="Left" Content="请求帮助 Help request:" Background="Transparent" BorderBrush="Transparent"/>
            <Label HorizontalAlignment="Left" Grid.Row="1" Content="故障代码 Error code:" Background="Transparent" BorderBrush="Transparent"/>
            <Label HorizontalAlignment="Left" Grid.Row="2" Content="故障信息 Error message:" Background="Transparent" BorderBrush="Transparent"/>
            <Label HorizontalAlignment="Left" Grid.Row="3" Content="故障描述 Error detail:" Background="Transparent" BorderBrush="Transparent"/>
            <TextBox Name="tbStartTime" IsReadOnly="True" Text="{Binding ErrorStartTime}" Grid.Column="1" Background="#D6D6D6" Margin="10,10,10,10" Grid.ColumnSpan="2"/>
            <TextBox Text="{Binding ErrorCode}" Grid.Row="1" Grid.Column="1" Background="#D6D6D6" Margin="10,10,10,10" Grid.ColumnSpan="2"/>
            <TextBox Text="{Binding ErrorMessage}" Grid.Row="2" Grid.Column="1" Background="#D6D6D6" Margin="10,10,10,10" Grid.ColumnSpan="2"/>
            <TextBox Text="{Binding ErrorDetail}" Grid.Row="3" Grid.Column="1" Background="#D6D6D6" Margin="10,10,10,10" Grid.ColumnSpan="2"/>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackPanel Margin="10">
                <Label Content="维修" Background="Transparent" BorderBrush="Transparent"/>
                <Grid  Background="Transparent">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>
                    <Image  Grid.RowSpan="2" Height="50" Opacity="1"  Source="/Luster.Motion.CommonUI;component/Views/Dialogs/card.png">
                    </Image>
                    <Label Grid.Row="0" Grid.Column="1" HorizontalAlignment="Left"  Content="OSS: 请刷卡开始维修" Background="Transparent" BorderBrush="Transparent" />
                    <Label  Grid.Row="1" Grid.Column="1"  Content="Scan badge to submit the selected option"  Background="Transparent" BorderBrush="Transparent"/>
                </Grid>
                <TextBlock Width="300" HorizontalAlignment="Left" Text="{Binding CardLog}"  TextWrapping="Wrap" Background="LightGray" Height="80"/>
                <Label Background="{Binding CardBkColor}" HorizontalAlignment="Left" Width="300" Height="15"/>
            </StackPanel>
            <GridSplitter Grid.Column="1" Width="1" Background="Black"/>
            <StackPanel Grid.Column="2" Margin="10">
                <Label Content="无需维修" Background="Transparent" BorderBrush="Transparent"/>
                <TextBlock Width="300" HorizontalAlignment="Center" Text="OP:清除故障，设备还原到生产模式" />
                <TextBlock Width="300" HorizontalAlignment="Center" Text="Clear error without repairing" />
                <Button 
                    Command="{Binding Path=DataContext.ClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    CommandParameter="Clear"
                    Focusable="False"
                    IsEnabled="{Binding IsButtonEnable}" 
                    Width="300" 
                    Content="&#x00a0;&#x00a0;&#x00a0;&#x00a0;&#x00a0;&#x00a0;&#x00a0;&#x00a0;确认-清掉故障&#x0a;Acknowledge-Clear error" 
                    Height="100" 
                    HorizontalContentAlignment="Center"  
                    Background="#518FFF"/>
                <Grid HorizontalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Width="200" HorizontalAlignment="Left" Text="剩余时间 Remaining time:" />
                    <TextBlock HorizontalAlignment="Left" Text="{Binding CountDown}" Grid.Column="1" />
                </Grid>

            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
