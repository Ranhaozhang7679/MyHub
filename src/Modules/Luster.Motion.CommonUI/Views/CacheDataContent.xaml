<UserControl
    x:Class="Luster.Motion.CommonUI.Views.CacheDataContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converter="clr-namespace:Luster.Motion.CommonUI.Converter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:lang="clr-namespace:Luster.Motion.Assests.Langs;assembly=Luster.Motion.Assests"
    xmlns:local="clr-namespace:Luster.Motion.CommonUI.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <converter:ProductStringConverter x:Key="ResultConverter" />
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <DataGrid
            Name="dgProduct"
            Grid.Row="1"
            Padding="-5"
            AlternationCount="2"
            BorderBrush="{DynamicResource BorderBrush}"
            CanUserSortColumns="False"
            ColumnHeaderStyle="{DynamicResource CustomDataGridColumnHeaderStyle}"
            HeadersVisibility="Column"
            ItemsSource="{Binding CacheDatas}"
            SelectionMode="Single">
            <DataGrid.RowStyle>
                <Style BasedOn="{StaticResource CustomDataGridRowStyle}" TargetType="DataGridRow">
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsRemoved}" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource DangerBrush}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="200"
                    MinWidth="55"
                    Binding="{Binding DataId}"
                    ElementStyle="{DynamicResource DataColumnTextElementStyleCenter}"
                    Header="{lang:Lang Key={x:Static lang:LangKeys.DataMark}}" />
                <DataGridTextColumn
                    Width="145"
                    MinWidth="145"
                    Binding="{Binding CreateTime, StringFormat='yyyy-MM-dd HH:mm:ss'}"
                    ElementStyle="{DynamicResource DataColumnTextElementStyleCenter}"
                    Header="{lang:Lang Key={x:Static lang:LangKeys.CreateTime}}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
