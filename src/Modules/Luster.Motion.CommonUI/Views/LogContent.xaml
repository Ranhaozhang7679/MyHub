<UserControl
    x:Class="Luster.Motion.CommonUI.Views.LogContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrl="clr-namespace:Luster.Control.Wpf.Motion.Controls;assembly=Luster.Control.Wpf.Motion"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:lang="clr-namespace:Luster.Motion.Assests.Langs;assembly=Luster.Motion.Assests"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    MinHeight="300"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="{DynamicResource RegionBrush}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Orientation="Horizontal">
            <hc:ButtonGroup
                Width="120"
                Margin="5"
                HorizontalAlignment="Left"
                VerticalAlignment="Center">
                <ToggleButton Content="Debug" IsChecked="{Binding DebugChecked}" />
                <ToggleButton Content="Info" IsChecked="{Binding InfoChecked}" />
            </hc:ButtonGroup>
            <hc:ElementGroup
                VerticalAlignment="Center"
                BorderBrush="{DynamicResource BorderBrush}"
                Layout="Stack"
                Visibility="{Binding ShowMonitor, Converter={StaticResource Boolean2VisibilityConverter}}">
                <TextBox
                    Width="120"
                    VerticalAlignment="Center"
                    FontSize="14"
                    FontWeight="Bold"
                    IsReadOnly="True"
                    Text="{Binding MonitorText}" />
                <Button
                    Padding="5,0"
                    Command="{Binding ClearMonitorCommand}"
                    Style="{StaticResource ButtonDanger}">
                    <Button.Content>
                        <TextBlock
                            FontSize="20"
                            Style="{StaticResource MotionIconMid}"
                            Text="&#xe620;" />
                    </Button.Content>
                </Button>
            </hc:ElementGroup>
        </StackPanel>
        <!-- 日志路径按钮 -->
        <Button
            Grid.Column="0"
            Width="80"
            Margin="10,0,10,0"
            HorizontalAlignment="Right"
            Content="日志路径"
            Command="{Binding OpenLogFolderCommand}"/>
        <Button
            Grid.Column="1"
            Width="80"
            Margin="0,0,10,0"
            HorizontalAlignment="Right"
            Command="{Binding ClearLogCommand}"
            Content="{lang:Lang Key={x:Static lang:LangKeys.Clear}}" />
        <ctrl:LogCtrl Grid.Row="1" Grid.ColumnSpan="2">
            <hc:Interaction.Triggers>
                <hc:EventTrigger EventName="Loaded">
                    <hc:EventToCommand Command="{Binding LoadedCommand}" PassEventArgsToCommand="True" />
                </hc:EventTrigger>
            </hc:Interaction.Triggers>
        </ctrl:LogCtrl>
    </Grid>
</UserControl>
