<UserControl
    x:Class="Luster.Common.Assets.Views.InfoInputDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ex="clr-namespace:Luster.Common.Assets.Extension"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:langs="clr-namespace:Luster.Common.Assets.Langs;assembly=Luster.Common.Assets"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="{DynamicResource RegionBrush}">
    <UserControl.Resources>
        <Style BasedOn="{StaticResource {x:Type Button}}" TargetType="Button">
            <Setter Property="Width" Value="78" />
            <Setter Property="Height" Value="32" />
        </Style>
    </UserControl.Resources>
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="150" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <hc:TextBox
            x:Name="txtText"
            Margin="5,10"
            VerticalAlignment="Stretch"
            hc:InfoElement.Necessary="True"
            hc:InfoElement.Placeholder="请输入文本信息（支持多行）"
            AcceptsReturn="True"
            Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
            TextWrapping="Wrap"
            VerticalScrollBarVisibility="Auto"
            HorizontalScrollBarVisibility="Disabled"
            MaxHeight="150"
            MinHeight="80" />
        <hc:Divider
            Grid.Row="1"
            Margin="0"
            LineStrokeDashArray="2,2"
            LineStrokeThickness="1"
            Orientation="Horizontal" />
        <Grid
            Grid.Row="2"
            Height="40"
            Background="WhiteSmoke">
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Button
                    MinWidth="60"
                    Margin="5"
                    Command="{Binding CloseCommand}"
                    CommandParameter="Cancel"
                    Content="{Binding BtnCancelText}" />
                <Button
                    MinWidth="60"
                    Margin="5"
                    Command="{Binding CloseCommand}"
                    CommandParameter="OK"
                    Content="{Binding BtnOKText}" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
