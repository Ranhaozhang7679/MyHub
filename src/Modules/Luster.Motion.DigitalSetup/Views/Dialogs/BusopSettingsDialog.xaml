<UserControl
    x:Class="Luster.Motion.DigitalSetup.Views.Dialogs.BusopSettingsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d"
    Height="280" d:DesignHeight="280" d:DesignWidth="480">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0"
                   Text="BUSOP 文件设置"
                   FontSize="16" FontWeight="Bold"
                   Margin="0,0,0,15" />

        <!-- xlsx 文件路径 -->
        <GroupBox Grid.Row="1" Header="Excel 文件路径" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding ExcelFilePath, UpdateSourceTrigger=PropertyChanged}"
                         hc:InfoElement.Placeholder="绝对路径或配方相对路径"
                         VerticalContentAlignment="Center" />
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="5,0,0,0">
                    <Button Content="浏览" Command="{Binding BrowseFileCommand}"
                            Width="60" Height="28" Margin="0,0,5,0" />
                    <Button Content="刷新Sheet" Command="{Binding RefreshSheetsCommand}"
                            Width="70" Height="28" />
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Sheet 页选择 -->
        <GroupBox Grid.Row="2" Margin="0,0,0,10">
            <GroupBox.Header>
                <TextBlock>
                    <Run Text="Sheet 页（"/>
                    <Run Text="{Binding CurrentSubItemName, Mode=OneWay}" FontWeight="Bold"/>
                    <Run Text="）"/>
                </TextBlock>
            </GroupBox.Header>
            <ComboBox ItemsSource="{Binding AvailableSheets}"
                      Text="{Binding CurrentSheetName, UpdateSourceTrigger=PropertyChanged}"
                      IsEditable="True"
                      hc:InfoElement.Placeholder="选择或输入 Sheet 页名称"
                      VerticalContentAlignment="Center" />
        </GroupBox>

        <!-- 占位 -->
        <Border Grid.Row="3" />

        <!-- 按钮 -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" Command="{Binding SaveCommand}"
                    Style="{StaticResource ButtonPrimary}"
                    Width="80" Height="32" Margin="0,0,10,0" />
            <Button Content="取消" Command="{Binding CancelCommand}"
                    Width="80" Height="32" />
        </StackPanel>
    </Grid>
</UserControl>