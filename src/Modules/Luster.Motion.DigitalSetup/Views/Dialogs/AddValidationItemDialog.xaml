<UserControl x:Class="Luster.Motion.DigitalSetup.Views.Dialogs.AddValidationItemDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0"
                   Text="请输入验证项名称："
                   FontSize="14"
                   Margin="0,0,0,10"/>
        
        <TextBox Grid.Row="1"
                 Text="{Binding ItemName, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                 hc:InfoElement.Placeholder="输入名称..."
                 Height="30"
                 Margin="0,0,0,15"/>
        
        <TextBlock Grid.Row="2"
                   Text="请选择验证类型："
                   FontSize="14"
                   Margin="0,0,0,10"/>
        
        <ComboBox Grid.Row="3"
                  ItemsSource="{Binding ValidationTypes}"
                  SelectedItem="{Binding SelectedValidationType, Mode=TwoWay}"
                  Height="30"
                  Margin="0,0,0,15">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="确定"
                    Command="{Binding ConfirmCommand}"
                    Style="{StaticResource ButtonPrimary}"
                    Width="70"
                    Height="28"
                    Margin="0,0,10,0"/>
            <Button Content="取消"
                    Command="{Binding CancelCommand}"
                    Width="70"
                    Height="28"/>
        </StackPanel>
    </Grid>
</UserControl>
