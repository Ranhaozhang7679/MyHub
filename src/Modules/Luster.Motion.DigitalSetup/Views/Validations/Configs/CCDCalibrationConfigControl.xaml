<UserControl x:Class="Luster.Motion.DigitalSetup.Views.Validations.Configs.CCDCalibrationConfigControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- CCD类型 -->
        <TextBlock Grid.Row="0" Grid.Column="0" 
                   Text="CCD类型：" 
                   VerticalAlignment="Center"
                   Margin="0,0,10,10"/>
        <ComboBox Grid.Row="0" Grid.Column="1"
                  SelectedItem="{Binding CCDType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  Height="28"
                  Margin="0,0,0,10">
            <ComboBoxItem Content="AreaScan"/>
            <ComboBoxItem Content="LineScan"/>
            <ComboBoxItem Content="Telecentric"/>
        </ComboBox>

        <!-- 像素尺寸 -->
        <TextBlock Grid.Row="1" Grid.Column="0" 
                   Text="像素尺寸 (μm)：" 
                   VerticalAlignment="Center"
                   Margin="0,0,10,10"/>
        <hc:NumericUpDown Grid.Row="1" Grid.Column="1"
                         Value="{Binding PixelSize, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Minimum="0.1"
                         Maximum="100"
                         Increment="0.1"
                         Margin="0,0,0,10"/>

        <!-- X分辨率 -->
        <TextBlock Grid.Row="2" Grid.Column="0" 
                   Text="X分辨率 (px)：" 
                   VerticalAlignment="Center"
                   Margin="0,0,10,10"/>
        <hc:NumericUpDown Grid.Row="2" Grid.Column="1"
                         Value="{Binding ResolutionX, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Minimum="1"
                         Maximum="10000"
                         Increment="1"
                         Margin="0,0,0,10"/>

        <!-- Y分辨率 -->
        <TextBlock Grid.Row="3" Grid.Column="0" 
                   Text="Y分辨率 (px)：" 
                   VerticalAlignment="Center"
                   Margin="0,0,10,0"/>
        <hc:NumericUpDown Grid.Row="3" Grid.Column="1"
                         Value="{Binding ResolutionY, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Minimum="1"
                         Maximum="10000"
                         Increment="1"/>
    </Grid>
</UserControl>
