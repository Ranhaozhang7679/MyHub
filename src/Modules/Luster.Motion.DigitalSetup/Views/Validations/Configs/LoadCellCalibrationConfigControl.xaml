<UserControl x:Class="Luster.Motion.DigitalSetup.Views.Validations.Configs.LoadCellCalibrationConfigControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- LoadCell类型 -->
        <TextBlock Grid.Row="0" Grid.Column="0" 
                   Text="LoadCell类型：" 
                   VerticalAlignment="Center"
                   Margin="0,0,10,10"/>
        <ComboBox Grid.Row="0" Grid.Column="1"
                  SelectedItem="{Binding LoadCellType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  Height="28"
                  Margin="0,0,0,10">
            <ComboBoxItem Content="Standard"/>
            <ComboBoxItem Content="HighPrecision"/>
            <ComboBoxItem Content="Miniature"/>
        </ComboBox>

        <!-- 校准值 -->
        <TextBlock Grid.Row="1" Grid.Column="0" 
                   Text="校准值 (N)：" 
                   VerticalAlignment="Center"
                   Margin="0,0,10,10"/>
        <hc:NumericUpDown Grid.Row="1" Grid.Column="1"
                         Value="{Binding CalibrationValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Minimum="0"
                         Maximum="10000"
                         Increment="1"
                         Margin="0,0,0,10"/>

        <!-- 容差 -->
        <TextBlock Grid.Row="2" Grid.Column="0" 
                   Text="容差范围 (%)：" 
                   VerticalAlignment="Center"
                   Margin="0,0,10,0"/>
        <hc:NumericUpDown Grid.Row="2" Grid.Column="1"
                         Value="{Binding Tolerance, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Minimum="0"
                         Maximum="1"
                         Increment="0.001"/>
    </Grid>
</UserControl>
