<UserControl  
    x:Class="Luster.Motion.DigitalSetup.Views.CommonButton"  
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"  
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"  
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
    xmlns:hc="https://handyorg.github.io/handycontrol"  
    xmlns:lang="clr-namespace:Luster.Motion.Assests.Langs;assembly=Luster.Motion.Assests"  
    xmlns:local="clr-namespace:Luster.Motion.DigitalSetup.Views"  
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"  
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True"
    d:DesignHeight="50"  
    d:DesignWidth="800"  
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <!-- 按钮行 -->
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!-- 定义三列，按钮列自动，图标列固定最小宽度，进度条填充剩余空间 -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" MinWidth="50" />
        </Grid.ColumnDefinitions>
        <!-- 按钮放在第二行 -->
        <Button Content="中止点检"
                Command="{Binding DataContext.EndCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Margin="5"
                Grid.Row="0"
                Grid.Column="0"/>
        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
            <Button Content="一键点检"
                    Command="{Binding DataContext.OneKeyCheckCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    CommandParameter="{Binding ItemModels}"
                    Margin="2" />
            <Grid Margin="2,0,0,0" Cursor="Hand" ToolTip="双击设置点检确认消息">
                <Grid.InputBindings>
                    <MouseBinding MouseAction="LeftDoubleClick"
                                  Command="{Binding DataContext.SetCheckMessageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                  CommandParameter="{Binding SelectedReportPage}"/>
                </Grid.InputBindings>
                <Ellipse Width="16" Height="16" Fill="White" Stroke="Black" StrokeThickness="1"/>
                <Path
                    Width="12"
                    Height="12"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Data="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-2h2v2zm0-4h-2V7h2v6z"
                    Fill="Black"
                    Stretch="Uniform"/>
            </Grid>
        </StackPanel>
        <!-- 进度条 -->
        <ProgressBar Value="{Binding DataContext.ProgressValue, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Margin="5"
                Grid.Row="0"
                Grid.Column="2"
                Height="20"
                Background="White"
                Foreground="Blue"/>
    </Grid>
</UserControl>
