using System.Windows.Controls;

namespace Luster.Motion.FiveAxis.UI.Views
{
    /// <summary>
    /// FiveAxisCalibContent.xaml 的交互逻辑（TES-158 阶段 1 五轴标定向导顶级页容器骨架）。
    /// VM 由 Prism ViewModelLocator.AutoWireViewModel 自动注入 <see cref="ViewModel.FiveAxisCalibContentVM"/>。
    /// </summary>
    public partial class FiveAxisCalibContent : UserControl
    {
        public FiveAxisCalibContent()
        {
            InitializeComponent();
        }
    }
}
