<Border
    x:Class="Luster.SimDevice.SubSystem.Views.LeftMenuContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:g="clr-namespace:Luster.Common.Assets.Global;assembly=Luster.Common.Assets"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:lang="clr-namespace:Luster.SimDevice.SubSystem.Langs"
    xmlns:local="clr-namespace:Luster.SimDevice.SubSystem.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    Width="220"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <Border
        Background="{DynamicResource RegionBrush}"
        CornerRadius="10"
        Effect="{StaticResource EffectShadow4}">
        <Grid Background="#F4F6FA">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <ToggleButton
                Width="130"
                Grid.Column="0"
                Margin="10,5"
                hc:IconElement.Geometry="{StaticResource HomeGeometry}"
                Command="{Binding HomeCommand}"
                Content="{lang:Lang Key={x:Static lang:LangKeys.Home}}"
                IsChecked="{Binding IsHomePage}">
                    <ToggleButton.Style>
                        <Style BasedOn="{StaticResource {x:Type ToggleButton}}" TargetType="ToggleButton">
                            <Style.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Cursor" Value="Hand" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ToggleButton.Style>
                </ToggleButton>
                <Grid Grid.Column="1">
                    <StackPanel>
                        <RadioButton 
                            Content="单调" 
                            Command="{Binding HomeCommand}"/>
                        <RadioButton
                            Content="分组"
                            Command="{Binding GroupDebugCommand}"/>
                        <RadioButton
                            Content="参数"
                            Command="{Binding ControlParaCommand}"/>
                    </StackPanel>
                </Grid>
            </Grid>
                  

            
            <hc:Divider
                Height="2"
                Margin="0"
                VerticalAlignment="Bottom"
                LineStrokeThickness="1" />
            <ListBox
                Name="lstDevice"
                Grid.Row="1"
                Margin="0,5"
                Padding="0"           
                Background="#F4F6FA"
                BorderThickness="0"
                ItemsSource="{Binding DeviceList}"
                SelectedItem="{Binding SelectedPage}"
                SelectionMode="Single"
                VirtualizingStackPanel.IsVirtualizing="True">

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="30" />
                                    <ColumnDefinition Width="110" />
                                    <ColumnDefinition Width="30" />
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                FontSize="20"
                                Style="{StaticResource IconMid}"
                                Text="{Binding Icon}" />
                                <hc:HighlightTextBlock
                                Grid.Column="1"
                                Margin="6,0,0,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                SourceText="{lang:Lang Key={Binding Name}}" />
                                <Border
                                Grid.Column="2"
                                Height="15"
                                MinWidth="20"
                                Margin="10,0,0,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="5">
                                    <TextBlock
                                    Margin="0,2"
                                    Padding="0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Foreground="{DynamicResource lusterColor}"
                                    Text="{Binding ItemNum}" />
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ItemNum}" Value="0">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.GroupStyle>
                        <GroupStyle ContainerStyle="{StaticResource MenuRightGroupItemStyle}" />
                    </ListBox.GroupStyle>
                    <ListBox.ItemContainerStyle>
                        <Style BasedOn="{StaticResource ListBoxItemBaseStyle}" TargetType="ListBoxItem">
                            <Setter Property="Margin" Value="0,2,25,2" />
                            <Setter Property="Height" Value="35" />
                            <Setter Property="Background" Value="#DEE0E7" />
                            <Setter Property="hc:BorderElement.CornerRadius" Value="0,5,5,0" />
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="true">
                                    <Setter Property="Background" Value="{DynamicResource SecondaryRegionBrush}" />
                                </Trigger>
                                <Trigger Property="IsSelected" Value="true">
                                    <Setter Property="Background" Value="{DynamicResource PrimaryBrush}" />
                                    <Setter Property="Foreground" Value="{DynamicResource TextIconBrush}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <hc:Interaction.Triggers>
                        <hc:EventTrigger EventName="SelectionChanged">
                            <hc:EventToCommand Command="{Binding SelectedCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ListBox}, Path=SelectedItem}" />
                        </hc:EventTrigger>
                    </hc:Interaction.Triggers>
                </ListBox>

                
               
  
            
        </Grid>
    </Border>

</Border>
