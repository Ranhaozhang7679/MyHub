<UserControl
    x:Class="Luster.Motion.SubSystem.Views.ProIndexContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converter1="clr-namespace:Luster.Motion.CommonUI.Converter;assembly=Luster.Motion.CommonUI"
    xmlns:ctrl="clr-namespace:Luster.Control.Wpf.Motion.Controls;assembly=Luster.Control.Wpf.Motion"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:g="clr-namespace:Luster.Motion.CommonUI;assembly=Luster.Motion.CommonUI"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:lang="clr-namespace:Luster.Motion.Assests.Langs;assembly=Luster.Motion.Assests"
    xmlns:local="clr-namespace:Luster.Motion.SubSystem.Views"
    xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:views="clr-namespace:Luster.Motion.CommonUI.Views;assembly=Luster.Motion.CommonUI"
    d:DesignHeight="600"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="{DynamicResource BackgroundBrush}"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="CountTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>
        <Style x:Key="YieldTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Margin" Value="0,3" />
        </Style>
        <Style x:Key="HighLightTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="32" />
            <Setter Property="Foreground" Value="#FFFFFF" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="10" />
        </Style>
        <Style x:Key="MaterialTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20" />
            <Setter Property="Foreground" Value="#000000" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Margin" Value="0,5" />
        </Style>

        <Style TargetType="{x:Type ProgressBar}">
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Height" Value="15" />
            <Setter Property="Background" Value="Green" />
            <Setter Property="FontSize" Value="10" />
            <Setter Property="Padding" Value="5,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ProgressBar}">
                        <Grid Background="{StaticResource lusterColor}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Grid Height="{TemplateBinding Height}">
                                <Border BorderThickness="1" Opacity="0.1" />
                                <Grid Margin="{TemplateBinding BorderThickness}">
                                    <Border x:Name="PART_Track" />
                                    <Grid
                                        x:Name="PART_Indicator"
                                        HorizontalAlignment="Left"
                                        ClipToBounds="True">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition x:Name="width1" />
                                            <ColumnDefinition x:Name="width2" Width="0" />
                                        </Grid.ColumnDefinitions>
                                        <Grid x:Name="Animation" RenderTransformOrigin="0.5,0.5">
                                            <Grid.RenderTransform>
                                                <TransformGroup>
                                                    <ScaleTransform ScaleX="1" ScaleY="-1" />
                                                    <SkewTransform AngleX="0" AngleY="0" />
                                                    <RotateTransform Angle="180" />
                                                    <TranslateTransform />
                                                </TransformGroup>
                                            </Grid.RenderTransform>
                                            <Border Background="{TemplateBinding Background}">
                                                <Viewbox
                                                    Margin="{TemplateBinding Padding}"
                                                    HorizontalAlignment="Left"
                                                    SnapsToDevicePixels="True"
                                                    StretchDirection="DownOnly">
                                                    <TextBlock
                                                        VerticalAlignment="Center"
                                                        FontSize="{TemplateBinding FontSize}"
                                                        Foreground="#ffffff"
                                                        RenderTransformOrigin="0.5,0.5"
                                                        SnapsToDevicePixels="True"
                                                        Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Value, StringFormat={}{0}%}">
                                                        <TextBlock.RenderTransform>
                                                            <TransformGroup>
                                                                <ScaleTransform ScaleX="-1" ScaleY="1" />
                                                                <SkewTransform AngleX="0" AngleY="0" />
                                                                <RotateTransform Angle="0" />
                                                                <TranslateTransform />
                                                            </TransformGroup>
                                                        </TextBlock.RenderTransform>
                                                    </TextBlock>
                                                </Viewbox>
                                            </Border>
                                            <Border Opacity="0.1" />
                                        </Grid>
                                    </Grid>
                                </Grid>
                            </Grid>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Determinate" />
                                    <VisualState x:Name="Indeterminate">
                                        <Storyboard RepeatBehavior="Forever">
                                            <PointAnimationUsingKeyFrames Storyboard.TargetName="Animation" Storyboard.TargetProperty="(UIElement.RenderTransformOrigin)">
                                                <EasingPointKeyFrame KeyTime="0:0:0" Value="0.5,0.5" />
                                                <EasingPointKeyFrame KeyTime="0:0:1.5" Value="1.95,0.5" />
                                                <EasingPointKeyFrame KeyTime="0:0:3" Value="0.5,0.5" />
                                            </PointAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                        </Grid>
                        <ControlTemplate.Triggers>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#c5c5c5" />
                            </Trigger>
                            <Trigger Property="IsIndeterminate" Value="true">
                                <Setter TargetName="width1" Property="Width" Value="0.25*" />
                                <Setter TargetName="width2" Property="Width" Value="0.725*" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="70" />
        </Grid.RowDefinitions>
        <!--  产品信息  -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid>
                <TextBlock
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="18"
                    Text="{Binding ProductResult}" />
                <lvc:PieChart
                    Name="lvc"
                    Width="320"
                    Height="320"
                    EasingFunction="{x:Null}"
                    InitialRotation="90"
                    MaxAngle="270"
                    Series="{Binding Series}"
                    Total="100" />
            </Grid>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Margin="10,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <!--<Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>-->
                    <TextBlock
                        HorizontalAlignment="Left"
                        Style="{StaticResource CountTextStyle}"
                        Text="{Binding RealTime}" />
                    <TextBlock
                        Grid.Column="1"
                        HorizontalAlignment="Right"
                        Style="{StaticResource CountTextStyle}"
                        Text="{Binding RunTime, StringFormat={}运行时间 {0} min}" />
                    <!--<TextBlock
                        Grid.Row="1"
                        Grid.Column="1"
                        HorizontalAlignment="Right"
                        Style="{StaticResource CountTextStyle}"
                        Text="{Binding IdleTime, StringFormat={}空闲时间 {0} min}" />-->
                </Grid>
                <Grid Grid.Row="1" Margin="5,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Border
                        Grid.Row="0"
                        Grid.Column="0"
                        Margin="5"
                        Background="#28AF15"
                        BorderThickness="1"
                        CornerRadius="5">
                        <StackPanel Orientation="Vertical">
                            <TextBlock Style="{StaticResource HighLightTextStyle}" Text="UPH : " />
                            <TextBlock
                                FontSize="42"
                                Style="{StaticResource HighLightTextStyle}"
                                Text="{Binding UPH}" />
                        </StackPanel>
                    </Border>
                    <Border
                        Grid.Row="0"
                        Grid.Column="1"
                        Margin="5"
                        Background="#146BB2"
                        BorderThickness="1"
                        CornerRadius="5">
                        <StackPanel Orientation="Vertical">
                            <TextBlock Style="{StaticResource HighLightTextStyle}" Text="CT : " />
                            <TextBlock
                                FontSize="42"
                                Style="{StaticResource HighLightTextStyle}"
                                Text="{Binding RealCT, StringFormat={}{0} s}" />
                        </StackPanel>
                    </Border>
                    <Border
                        Grid.Row="1"
                        Grid.Column="0"
                        Margin="5"
                        Background="Black"
                        BorderThickness="1"
                        CornerRadius="5">
                        <StackPanel Orientation="Vertical">
                            <TextBlock Style="{StaticResource HighLightTextStyle}" Text="{lang:Lang Key={x:Static lang:LangKeys.InputQty}}" />
                            <TextBlock
                                FontSize="42"
                                Style="{StaticResource HighLightTextStyle}"
                                Text="{Binding ProductCount}" />
                        </StackPanel>
                    </Border>
                    <Border
                        Grid.Row="1"
                        Grid.Column="1"
                        Margin="5"
                        Background="Black"
                        BorderThickness="1"
                        CornerRadius="5">
                        <StackPanel Orientation="Vertical">
                            <TextBlock Style="{StaticResource HighLightTextStyle}" Text="{lang:Lang Key={x:Static lang:LangKeys.CropRate}}" />
                            <TextBlock
                                FontSize="42"
                                Style="{StaticResource HighLightTextStyle}"
                                Text="{Binding WorkRate, StringFormat={}{0}%}" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
        <Grid Grid.Row="1" Margin="5,0">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock
                Grid.Row="0"
                Grid.Column="0"
                Margin="10,0,0,0"
                Foreground="Green"
                Style="{StaticResource YieldTextStyle}"
                Text="{Binding OkCount, StringFormat={}OK {0}}" />

            <TextBlock
                Grid.Row="0"
                Grid.Column="1"
                HorizontalAlignment="Center"
                Foreground="Green"
                Style="{StaticResource YieldTextStyle}"
                Text="{Binding Yield, StringFormat={}Yield {0}%}" />
            <StackPanel
                Grid.Column="2"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <TextBlock
                    Grid.Row="0"
                    Margin="0,0,20,0"
                    Foreground="Red"
                    Style="{StaticResource YieldTextStyle}"
                    Text="{Binding Toss, StringFormat={}Toss {0}}" />
                <TextBlock
                    Grid.Row="0"
                    Grid.Column="2"
                    Margin="0,0,10,0"
                    HorizontalAlignment="Right"
                    Foreground="Red"
                    Style="{StaticResource YieldTextStyle}"
                    Text="{Binding NgCount, StringFormat={}NG {0}}" />
            </StackPanel>

            <Grid Grid.Row="1" Grid.ColumnSpan="3">
                <ProgressBar
                    Height="32"
                    BorderThickness="0"
                    Maximum="100"
                    Value="{Binding Yield}" />
            </Grid>
        </Grid>
        <Grid Grid.Row="2" Margin="10,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ItemsControl ItemsSource="{Binding Materials}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <hc:UniformSpacingPanel Orientation="Horizontal" Spacing="10" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel MinWidth="80" Orientation="Vertical">
                                <TextBlock
                                    FontSize="18"
                                    Style="{StaticResource MaterialTextStyle}"
                                    Text="{Binding MaterialName}" />
                                <TextBlock Style="{StaticResource MaterialTextStyle}" Text="{Binding Count}" />
                            </StackPanel>
                            <!--<StackPanel
                                Grid.Column="1"
                                MinWidth="80"
                                Orientation="Vertical">
                                <TextBlock
                                    FontSize="18"
                                    Style="{StaticResource MaterialTextStyle}"
                                    Text="抛料率" />
                                <TextBlock Style="{StaticResource MaterialTextStyle}" Text="{Binding Rate, StringFormat={}{0}%}" />
                            </StackPanel>-->
                            <hc:Divider
                                Grid.Column="2"
                                Width="2"
                                Margin="5,0"
                                Padding="0,10"
                                LineStroke="Gray"
                                Orientation="Vertical" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Button
                Grid.Column="1"
                Width="150"
                Height="60"
                VerticalAlignment="Center"
                Command="{Binding ResetProductCommand}"
                Content="{lang:Lang Key={x:Static lang:LangKeys.ResetCapacity}}"
                FontSize="24"
                Foreground="Black"
                IsEnabled="{Binding BtnEnable}"
                Style="{StaticResource ButtonBaseStyle}" />
        </Grid>
    </Grid>
</UserControl>
