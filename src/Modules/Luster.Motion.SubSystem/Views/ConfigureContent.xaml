<UserControl
    x:Class="Luster.Motion.SubSystem.Views.ConfigureContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:lang="clr-namespace:Luster.Motion.Assests.Langs;assembly=Luster.Motion.Assests"
    xmlns:local="clr-namespace:Luster.Motion.SubSystem.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="{DynamicResource SecondaryRegionBrush}"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="250" />
        </Grid.ColumnDefinitions>
        <ContentControl x:Name="ConfigurationContent" prism:RegionManager.RegionName="ConfigurationRegion" />
        <StackPanel Grid.Column="1" Background="#F5F6FA">
            <Label
                Margin="0,40,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                hc:IconElement.Geometry="{StaticResource ConfigGeometry}"
                Background="#F5F6FA"
                BorderThickness="0"
                Content="{lang:Lang Key={x:Static lang:LangKeys.Configure}}"
                FontSize="20" />
            <!--<Border Background="#DDE0E7" CornerRadius="0 5 5 0" Margin="0 2 30 0" Height="55"/>
            <Border Background="#DDE0E7" CornerRadius="0 5 5 0" Margin="0 10 30 0" Height="55"/>
            <Border Background="#DDE0E7" CornerRadius="0 5 5 0" Margin="0 10 30 0" Height="55"/>
            <Border Background="#DDE0E7" CornerRadius="0 5 5 0" Margin="0 10 30 0" Height="55"/>-->
            <ListBox
                x:Name="listoperate"
                VerticalAlignment="Center"
                Background="#F5F6FA"
                BorderBrush="Transparent"
                ItemsSource="{Binding Pages}"
                Style="{StaticResource ListBoxCustom}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <RadioButton
                            Margin="0,10,30,0"
                            Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                            CommandParameter="{Binding}"
                            IsChecked="{Binding IsSelected}"
                            Style="{StaticResource OperateRadioStyle}">
                            <StackPanel>
                                <StackPanel>
                                    <TextBlock
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="15"
                                        Foreground="Black"
                                        Text="{lang:Lang Key={Binding Name}}" />
                                </StackPanel>
                            </StackPanel>
                        </RadioButton>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</UserControl>
