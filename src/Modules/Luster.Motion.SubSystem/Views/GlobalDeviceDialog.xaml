<Window x:Class="Luster.Motion.SubSystem.Views.DeviceDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:Luster.Motion.SubSystem.Views"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="一键屏蔽"
        Width="700"
        Height="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Window.Resources>
        <views:BoolToEnableDisableConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock
            Grid.Row="0"
            Margin="10"
            FontSize="14"
            FontWeight="Bold"
            Text="Bool类型全局变量列表" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10,0,10,10">
            <TextBlock
                VerticalAlignment="Center"
                Text="搜索:" />
            <TextBox
                Width="200"
                Margin="10,0,0,0"
                Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                Padding="5" />
        </StackPanel>
        <DataGrid
            Grid.Row="2"
            Margin="10,0,10,10"
            AutoGenerateColumns="False"
            HeadersVisibility="Column"
            IsReadOnly="True"
            ItemsSource="{Binding FilteredVariables}"
            SelectedItem="{Binding SelectedVariable}"
            SelectionMode="Single">
            <DataGrid.Columns>
                <!-- 勾选框列 -->
                <DataGridTemplateColumn Width="Auto" Header="选择">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox 
                                IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                Checked="CheckBox_Checked"
                                Unchecked="CheckBox_Unchecked"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn
                    Width="*"
                    Header="变量名称"
                    Binding="{Binding Name}" />
                <DataGridTextColumn
                    Width="100"
                    Header="类型"
                    Binding="{Binding TypeName}" />
                <DataGridTextColumn
                    Width="80"
                    Header="当前值">
                    <DataGridTextColumn.Binding>
                        <Binding Path="Value">
                            <Binding.Converter>
                                <views:BoolToTextConverter TrueText="true" FalseText="false" />
                            </Binding.Converter>
                        </Binding>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Border Grid.Row="3" Background="#F5F5F5" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Vertical">
                    <TextBlock 
                        Text="{Binding SelectedVariable.Name, StringFormat='选中变量: {0}'}" 
                        Visibility="{Binding IsVariableSelected, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <TextBlock 
                        Text="{Binding SelectedVariables.Count, StringFormat='已选择 {0} 个变量'}" 
                        Visibility="{Binding HasSelectedVariables, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Margin="0,5,0,0"
                        Foreground="Blue"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button
                        Content="确定"
                        Width="80"
                        Margin="0,0,10,0"
                        IsEnabled="{Binding HasSelectedVariables}"
                        Click="OkButton_Click" />
                    <Button
                        Content="取消"
                        Width="80"
                        Click="CancelButton_Click" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>