<UserControl
    x:Class="Luster.Motion.SubSystem.Views.DeviceDebugContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dlang="clr-namespace:Luster.SimDevice.SubSystem.Langs;assembly=Luster.SimDevice.SubSystem"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:lang="clr-namespace:Luster.Motion.Assests.Langs;assembly=Luster.Motion.Assests"
    xmlns:local="clr-namespace:Luster.Motion.SubSystem.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="{DynamicResource RegionBrush}"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style
            x:Key="DigitalStyle"
            BasedOn="{StaticResource ToggleButtonFlip.Small}"
            TargetType="ToggleButton">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="1" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ListBox
            Name="lstStation"
            Grid.RowSpan="3"
            Grid.Column="0"
            MinWidth="120"
            Padding="10,5"
            Background="AliceBlue"
            BorderThickness="0"
            ItemsSource="{Binding Stations}"
            SelectedItem="{Binding SelectedStation}"
            SelectionMode="Single"
            VirtualizingStackPanel.IsVirtualizing="True">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <hc:UniformSpacingPanel
                        Background="Transparent"
                        Orientation="Vertical"
                        Spacing="10" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock
                        Width="80"
                        HorizontalAlignment="Center"
                        Background="Transparent"
                        Foreground="{Binding Path=Foreground, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ListBoxItem}}"
                        Style="{DynamicResource TextBlockDefault}"
                        Text="{Binding StationName}"
                        TextAlignment="Center" />
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style BasedOn="{StaticResource ListBoxItemBaseStyle}" TargetType="ListBoxItem">
                    <Setter Property="Margin" Value="0,0,5,0" />
                    <Setter Property="Background" Value="#DEE0E7" />
                    <Setter Property="hc:BorderElement.CornerRadius" Value="5" />
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Background" Value="{DynamicResource SecondaryRegionBrush}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="true">
                            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource TextIconBrush}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
            <hc:Interaction.Triggers>
                <hc:EventTrigger EventName="SelectionChanged">
                    <hc:EventToCommand Command="{Binding SelectedCommand}" CommandParameter="{Binding ElementName=lstStation, Path=SelectedItem}" />
                </hc:EventTrigger>
            </hc:Interaction.Triggers>
        </ListBox>
        <ListBox
            Grid.Column="1"
            Margin="10,5"
            Padding="0"
            BorderThickness="0"
            ItemsSource="{Binding DeviceTypes}"
            SelectionMode="Single"
            VirtualizingStackPanel.IsVirtualizing="True">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <hc:UniformSpacingPanel
                        Background="Transparent"
                        Orientation="Horizontal"
                        Spacing="10" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock
                        Width="80"
                        HorizontalAlignment="Center"
                        Background="Transparent"
                        Foreground="{Binding Path=Foreground, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ListBoxItem}}"
                        Style="{DynamicResource TextBlockDefault}"
                        Text="{dlang:Lang Key={Binding DeviceType}}"
                        TextAlignment="Center" />
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style BasedOn="{StaticResource ListBoxItemBaseStyle}" TargetType="ListBoxItem">
                    <Setter Property="Margin" Value="0,0,5,0" />
                    <Setter Property="Background" Value="#DEE0E7" />
                    <Setter Property="hc:BorderElement.CornerRadius" Value="5" />
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Background" Value="{DynamicResource SecondaryRegionBrush}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="true">
                            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource TextIconBrush}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
            <hc:Interaction.Triggers>
                <hc:EventTrigger EventName="SelectionChanged">
                    <hc:EventToCommand Command="{Binding SelectTypeCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ListBox}, Path=SelectedItem}" />
                </hc:EventTrigger>
            </hc:Interaction.Triggers>
        </ListBox>
        <hc:Divider
            Grid.Row="1"
            Grid.Column="1"
            Height="3"
            Margin="5,2"
            LineStrokeThickness="1" />

        <!--  IO 信号  -->
        <Grid
            Grid.Row="3"
            Grid.Column="1"
            Margin="5"
            Visibility="{Binding IOVisible, Converter={StaticResource Boolean2VisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <TextBlock
                Grid.Column="0"
                Margin="5,0"
                FontSize="20"
                FontWeight="Bold"
                Text="输入信号" />
            <TextBlock
                Grid.Column="1"
                Margin="5,0"
                FontSize="20"
                FontWeight="Bold"
                Text="输出信号" />

            <ScrollViewer
                Grid.Row="1"
                Grid.Column="0"
                Margin="0,5">
                <ItemsControl ItemsSource="{Binding InputIOs}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <hc:UniformSpacingPanel
                                HorizontalAlignment="Left"
                                VerticalAlignment="Top"
                                ChildWrapping="Wrap"
                                Orientation="Horizontal"
                                ScrollViewer.VerticalScrollBarVisibility="Visible" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border
                                Name="BorderItem"
                                Height="36"
                                Margin="2"
                                Padding="5,4"
                                BorderThickness="1"
                                Style="{StaticResource BorderRegion}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="35" />
                                        <ColumnDefinition Width="180" />
                                        <ColumnDefinition Width="30" />
                                    </Grid.ColumnDefinitions>
                                    <Border HorizontalAlignment="Left">
                                        <TextBlock
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Center"
                                            FontSize="12"
                                            FontWeight="Bold"
                                            Foreground="{DynamicResource PrimaryBrush}"
                                            Text="{Binding Index, StringFormat={}X{0}}" />
                                    </Border>
                                    <TextBlock
                                        Grid.Column="1"
                                        Margin="5,0"
                                        VerticalAlignment="Center"
                                        FontSize="12"
                                        Text="{Binding Name}"
                                        ToolTip="{Binding Name}"
                                        Visibility="{Binding IsSelected, Converter={StaticResource Boolean2VisibilityReConverter}}" />
                                    <ToggleButton
                                        Grid.Column="2"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        BorderThickness="0"
                                        IsChecked="{Binding DigitalValue}"
                                        IsEnabled="{Binding Path=DataContext.IsReal, Converter={StaticResource Boolean2BooleanReConverter}, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ListBox}}"
                                        Style="{StaticResource DigitalStyle}">
                                        <hc:StatusSwitchElement.CheckedElement>
                                            <Border Background="Green" />
                                        </hc:StatusSwitchElement.CheckedElement>
                                        <Border Background="Gray" />
                                    </ToggleButton>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            <ScrollViewer
                Grid.Row="1"
                Grid.Column="1"
                Margin="0,5">
                <ItemsControl ItemsSource="{Binding OutputIOs}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <hc:UniformSpacingPanel
                                HorizontalAlignment="Left"
                                VerticalAlignment="Top"
                                ChildWrapping="Wrap"
                                Orientation="Vertical"
                                ScrollViewer.VerticalScrollBarVisibility="Visible" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border
                                Name="BorderItem"
                                Height="36"
                                Margin="2"
                                Padding="5,4"
                                BorderThickness="1"
                                Style="{StaticResource BorderRegion}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="35" />
                                        <ColumnDefinition Width="180" />
                                        <ColumnDefinition Width="30" />
                                    </Grid.ColumnDefinitions>
                                    <Border HorizontalAlignment="Left">
                                        <TextBlock
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Center"
                                            FontSize="12"
                                            FontWeight="Bold"
                                            Foreground="{DynamicResource PrimaryBrush}"
                                            Text="{Binding Index, StringFormat={}Y{0}}" />
                                    </Border>
                                    <TextBlock
                                        Grid.Column="1"
                                        Margin="5,0"
                                        VerticalAlignment="Center"
                                        FontSize="12"
                                        Text="{Binding Name}"
                                        ToolTip="{Binding Name}"
                                        Visibility="{Binding IsSelected, Converter={StaticResource Boolean2VisibilityReConverter}}" />
                                    <ToggleButton
                                        Grid.Column="2"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        BorderThickness="0"
                                        IsChecked="{Binding DigitalValue}"
                                        IsEnabled="{Binding Path=DataContext.IsReal, Converter={StaticResource Boolean2BooleanReConverter}, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ListBox}}"
                                        Style="{StaticResource DigitalStyle}">
                                        <hc:StatusSwitchElement.CheckedElement>
                                            <Border Background="Green" />
                                        </hc:StatusSwitchElement.CheckedElement>
                                        <Border Background="Gray" />
                                    </ToggleButton>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
