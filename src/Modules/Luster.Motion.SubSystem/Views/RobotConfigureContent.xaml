<UserControl
    x:Class="Luster.Motion.SubSystem.Views.RobotConfigureContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:lang="clr-namespace:Luster.Motion.Assests.Langs;assembly=Luster.Motion.Assests"
    xmlns:local="clr-namespace:Luster.Motion.SubSystem.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="{DynamicResource SecondaryRegionBrush}"
    mc:Ignorable="d">
    <Grid Background="{DynamicResource GridBackgroundColor}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400" />
            <ColumnDefinition Width="30" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Border
            Margin="20,15,0,20"
            Background="White"
            CornerRadius="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <StackPanel Margin="15,5,15,0">
                        <TextBlock
                            VerticalAlignment="Center"
                            DockPanel.Dock="Left"
                            Text="{lang:Lang Key={x:Static lang:LangKeys.RobotAction}}" />
                        <hc:Divider Margin="0,3,0,10" />
                        <ListBox
                            Name="listRobotOut"
                            Margin="10"
                            BorderThickness="0"
                            ItemsSource="{Binding ListRobotCommands}"
                            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                            Style="{DynamicResource ListBoxCustom}">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="4" Rows="2" />
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Height="55" Margin="0,5,0,0">
                                        <RadioButton
                                            Background="{Binding BtnColor}"
                                            IsEnabled="{Binding IsEdit}"
                                            Style="{StaticResource StatusRadioStyle}">
                                            <RadioButton.InputBindings>
                                                <MouseBinding
                                                    Command="{Binding DataContext.EditOPerateCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                    CommandParameter="{Binding}"
                                                    MouseAction="LeftDoubleClick" />
                                            </RadioButton.InputBindings>
                                        </RadioButton>
                                        <TextBlock
                                            Margin="0,5,0,0"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Text="{Binding Name}"
                                            Visibility="{Binding IsNameVisible}" />
                                        <TextBlock
                                            Margin="0,5,0,0"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Text="{Binding SelectDevice}"
                                            TextWrapping="Wrap"
                                            Visibility="{Binding IsDeviceVisible}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="1">
                    <StackPanel Margin="15,5,15,0">
                        <TextBlock
                            VerticalAlignment="Center"
                            DockPanel.Dock="Left"
                            Text="{lang:Lang Key={x:Static lang:LangKeys.RobotStatus}}" />
                        <hc:Divider Margin="0,3,0,10" />
                        <ListBox
                            Name="listRobotIn"
                            Margin="10"
                            BorderThickness="0"
                            ItemsSource="{Binding ListRobotStatus}"
                            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                            Style="{DynamicResource ListBoxCustom}">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="4" Rows="2" />
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Height="55" Margin="0,5,0,0">
                                        <RadioButton
                                            Background="{Binding BtnColor}"
                                            IsEnabled="{Binding IsEdit}"
                                            Style="{StaticResource StatusRadioStyle}">
                                            <RadioButton.InputBindings>
                                                <MouseBinding
                                                    Command="{Binding DataContext.EditOPerateCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                    CommandParameter="{Binding}"
                                                    MouseAction="LeftDoubleClick" />
                                            </RadioButton.InputBindings>
                                        </RadioButton>
                                        <TextBlock
                                            Margin="0,5,0,0"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Text="{Binding Name}"
                                            Visibility="{Binding IsNameVisible}" />
                                        <TextBlock
                                            Margin="0,5,0,0"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Text="{Binding SelectDevice}"
                                            TextWrapping="Wrap"
                                            Visibility="{Binding IsDeviceVisible}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="2">
                    <StackPanel Margin="15,5,15,0">
                        <TextBlock
                            VerticalAlignment="Center"
                            DockPanel.Dock="Left"
                            Text="{lang:Lang Key={x:Static lang:LangKeys.RobotAction2}}" />
                        <hc:Divider Margin="0,3,0,10" />
                        <ListBox
                            Name="listRobotOut2"
                            Margin="10"
                            BorderThickness="0"
                            ItemsSource="{Binding ListRobotCommands2}"
                            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                            Style="{DynamicResource ListBoxCustom}">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="4" Rows="2" />
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Height="55" Margin="0,5,0,0">
                                        <RadioButton
                                            Background="{Binding BtnColor}"
                                            IsEnabled="{Binding IsEdit}"
                                            Style="{StaticResource StatusRadioStyle}">
                                            <RadioButton.InputBindings>
                                                <MouseBinding
                                                    Command="{Binding DataContext.EditOPerateCommand2, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                    CommandParameter="{Binding}"
                                                    MouseAction="LeftDoubleClick" />
                                            </RadioButton.InputBindings>
                                        </RadioButton>
                                        <TextBlock
                                            Margin="0,5,0,0"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Text="{Binding Name}"
                                            Visibility="{Binding IsNameVisible}" />
                                        <TextBlock
                                            Margin="0,5,0,0"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Text="{Binding SelectDevice}"
                                            TextWrapping="Wrap"
                                            Visibility="{Binding IsDeviceVisible}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="3">
                    <StackPanel Margin="15,5,15,0">
                        <TextBlock
                            VerticalAlignment="Center"
                            DockPanel.Dock="Left"
                            Text="{lang:Lang Key={x:Static lang:LangKeys.RobotStatus2}}" />
                        <hc:Divider Margin="0,3,0,10" />
                        <ListBox
                            Name="listRobotIn2"
                            Margin="10"
                            BorderThickness="0"
                            ItemsSource="{Binding ListRobotStatus2}"
                            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                            Style="{DynamicResource ListBoxCustom}">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="4" Rows="2" />
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Height="55" Margin="0,5,0,0">
                                        <RadioButton
                                            Background="{Binding BtnColor}"
                                            IsEnabled="{Binding IsEdit}"
                                            Style="{StaticResource StatusRadioStyle}">
                                            <RadioButton.InputBindings>
                                                <MouseBinding
                                                    Command="{Binding DataContext.EditOPerateCommand2, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                    CommandParameter="{Binding}"
                                                    MouseAction="LeftDoubleClick" />
                                            </RadioButton.InputBindings>
                                        </RadioButton>
                                        <TextBlock
                                            Margin="0,5,0,0"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Text="{Binding Name}"
                                            Visibility="{Binding IsNameVisible}" />
                                        <TextBlock
                                            Margin="0,5,0,0"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Text="{Binding SelectDevice}"
                                            TextWrapping="Wrap"
                                            Visibility="{Binding IsDeviceVisible}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
