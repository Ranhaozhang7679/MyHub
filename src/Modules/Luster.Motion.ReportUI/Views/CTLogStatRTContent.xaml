<UserControl x:Class="Luster.Motion.ReportUI.Views.CTLogStatRTContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部标题栏 -->
        <Border Grid.Row="0" Background="#F5F5F5" Padding="15,10" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <TextBlock Text="当前在制产品分布统计" FontSize="18" FontWeight="Bold" Foreground="#333333"/>
        </Border>

        <!-- 主内容区域：Tab页控件 -->
        <hc:TabControl Grid.Row="1"
                       ItemsSource="{Binding TabPages}"
                       SelectedItem="{Binding SelectedTabPage}"
                       Style="{StaticResource TabControlInLine}">

            <!-- Tab页标题样式 -->
            <hc:TabControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="15,8">
                        <TextBlock Text="{Binding TabName}" FontSize="14" FontWeight="Medium" Foreground="Black"/>
                    </Grid>
                </DataTemplate>
            </hc:TabControl.ItemTemplate>

            <!-- Tab页内容：每个Tab页包含一个CT统计表格 -->
            <hc:TabControl.ContentTemplate>
                <DataTemplate>
                    <Border Background="White" Padding="10">
                        <!-- 使用DataGrid，支持列冻结和横向滚动 -->
                        <DataGrid x:Name="CTDataGrid"
                                  ItemsSource="{Binding GridRows}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  CanUserReorderColumns="False"
                                  CanUserResizeRows="False"
                                  SelectionMode="Single"
                                  SelectionUnit="Cell"
                                  GridLinesVisibility="All"
                                  HeadersVisibility="None"
                                  RowHeaderWidth="0"
                                  IsReadOnly="True"
                                  FrozenColumnCount="5"
                                  Background="White"
                                  BorderBrush="#CCCCCC"
                                  BorderThickness="1"
                                  LoadingRow="DataGrid_LoadingRow"
                                  Loaded="DataGrid_Loaded">

                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Height" Value="35"/>
                                    <Style.Triggers>
                                        <!-- TargetCT行：浅绿色 -->
                                        <DataTrigger Binding="{Binding RowType}" Value="TargetCT">
                                            <Setter Property="Background" Value="#C8E6C9"/>
                                        </DataTrigger>
                                        <!-- 表头行：浅蓝色，设置更大的高度支持换行 -->
                                        <DataTrigger Binding="{Binding RowType}" Value="Header">
                                            <Setter Property="Background" Value="#BBDEFB"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                            <Setter Property="Height" Value="70"/>
                                        </DataTrigger>
                                        <!-- 数据行：白色 -->
                                        <DataTrigger Binding="{Binding RowType}" Value="Data">
                                            <Setter Property="Background" Value="White"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>

                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                    <Setter Property="BorderThickness" Value="0,0,1,0"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                                    <Style.Triggers>
                                        <!-- 第2行（表头行）的第5列（等待时间）标黄 -->
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding RowType}" Value="Header"/>
                                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Column.DisplayIndex}" Value="4"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Background" Value="Yellow"/>
                                        </MultiDataTrigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{x:Null}"/>
                                            <Setter Property="Foreground" Value="Black"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.CellStyle>

                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#F5F5F5"/>
                                    <Setter Property="Foreground" Value="#333333"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                                    <Setter Property="Height" Value="30"/>
                                    <Setter Property="BorderBrush" Value="#CCCCCC"/>
                                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                        </DataGrid>
                    </Border>
                </DataTemplate>
            </hc:TabControl.ContentTemplate>
        </hc:TabControl>
    </Grid>
</UserControl>
